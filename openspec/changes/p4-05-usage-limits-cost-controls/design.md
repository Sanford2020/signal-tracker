# Design

`usage_events` records `workspace_id`, usage type, amount, and metadata.

Tracked usage types:

- `ai_extraction`
- `source_check`

Limits are configured from settings and enforced monthly.
