# Design

`notification_channels` stores workspace channel configs.
`notification_delivery_attempts` stores one delivery attempt per alert/channel.

The MVP provider returns success deterministically and stores payload metadata.
