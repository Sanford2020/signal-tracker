# P3-02 Source Provider Framework

The source checker should support real connectors without rewriting source check orchestration. This change introduces a provider registry behind the existing checker interface.
