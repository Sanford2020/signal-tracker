# H4-04 Environment Readiness Check

Hosted deployment failures are often caused by missing or unsafe environment variables. This change adds a static readiness script that checks env files before deployment.
