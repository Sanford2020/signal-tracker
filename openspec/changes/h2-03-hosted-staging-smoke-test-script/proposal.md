# H2-03 Hosted Staging Smoke Test Script

After selecting Render and adding a staging auth guard, operators need a repeatable remote smoke test that runs against hosted URLs.

This change adds a PowerShell script for backend health, request-id tracing, bootstrap guard behavior, and optional frontend availability.
