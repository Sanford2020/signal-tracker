# Design

Reports render from the same daily and weekly briefing data used by the UI.

Endpoints:

- `GET /api/v1/reports/daily.md`
- `GET /api/v1/reports/weekly.md`
- `GET /api/v1/reports/daily.pdf`
- `GET /api/v1/reports/weekly.pdf`
