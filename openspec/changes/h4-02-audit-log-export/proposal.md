# H4-02 Audit Log Export

Audit events are now stored and visible in the UI. Operators also need a portable export for incident review and handoff.

This change adds a CSV export endpoint and a Workspace page export action.
