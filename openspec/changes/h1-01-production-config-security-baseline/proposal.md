# H1-01 Production Config and Security Baseline

After P4, Signal Tracker has the first commercial surface area: workspaces, collaboration, notifications, exports, and usage controls. Before deeper product iteration, the runtime needs a safer deployment contract.

This change makes deployment-sensitive settings explicit, keeps local development friction low, and blocks the most obvious production misconfigurations.
